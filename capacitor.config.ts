import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sonora.demoapp',
  appName: 'demo-deploy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
